const { chromium } = require('C:/Users/ezequ/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert = require('node:assert/strict');
(async()=>{
 const browser=await chromium.launch({channel:'chrome',headless:true});
 const page=await browser.newPage({viewport:{width:1440,height:1000}});
 const errors=[];page.on('pageerror',e=>errors.push(e.message));
 await page.goto('http://localhost:5173/prototipos/obra-compras.html');
 await page.getByText('1–40 de 240 materiales',{exact:true}).waitFor();
 await page.screenshot({path:'output/obra-prototype-A.png',fullPage:true});
 await page.locator('#volume').selectOption('1585');
 await page.locator('#next').click();
 assert.match(await page.locator('#range').innerText(),/41–80 de 1.585/);
 await page.locator('#search').fill('MAT-1585');
 assert.match(await page.locator('#range').innerText(),/1–1 de 1/);
 await page.locator('#search').fill('');
 await page.locator('#page').fill('40');await page.locator('#page').dispatchEvent('change');
 assert.match(await page.locator('#range').innerText(),/1.561–1.585/);
 await page.locator('#volume').selectOption('240');
 await page.locator('[data-layout="B"]').click();
 await page.screenshot({path:'output/obra-prototype-B.png',fullPage:true});
 await page.locator('#inspector [data-product]').selectOption({label:'Garmin GPSMAP 1223xsv'});
 await page.locator('#inspector [data-define]').click();
 assert.equal(await page.locator('#inspector h2').innerText(),'Garmin GPSMAP 1223xsv');
 await page.locator('[data-layout="C"]').click();
 await page.locator('#theme').click();
 await page.screenshot({path:'output/obra-prototype-C.png',fullPage:true,animations:'disabled'});
 assert(['rgb(255, 255, 255)','rgb(231, 237, 243)'].includes(await page.locator('#theme').evaluate(el=>getComputedStyle(el).backgroundColor)));
 await page.locator('[data-view="Compras"]').click();
 await page.locator('#allPage').check();
 await page.locator('#bulkAction').click();
 assert(await page.locator('#draft').evaluate(el=>el.open));
 assert((await page.locator('#draftBody h3').count())>0);
 await page.locator('[data-close="draft"]').click();
 await page.locator('[data-view="Pañol"]').click();
 assert.equal(await page.locator('#scope').inputValue(),'deliver');
 await page.locator('[data-view="Costos"]').click();
 assert.match(await page.locator('#costnote').innerText(),/USD.*ARS/);
 await page.setViewportSize({width:1280,height:900});
 assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));
 await page.setViewportSize({width:768,height:1024});
 assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));
 await page.locator('[data-layout="B"]').click();await page.locator('[data-view="Obra"]').click();
 await page.locator('.queue-item').first().click();
 assert(await page.locator('#detail').evaluate(el=>el.open));
 await page.keyboard.press('Escape');
 assert(!(await page.locator('#detail').evaluate(el=>el.open)));
 await page.keyboard.press('/');assert.equal(await page.locator('#search').evaluate(el=>el===document.activeElement),true);
 await page.setViewportSize({width:390,height:844});
 assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));
 assert.deepEqual(errors,[]);
 console.log('PASS: pagination 1585, global search, definition, selection, supplier draft, role views, themes, 1280/tablet/mobile, keyboard, no JS errors');
 await browser.close();
})().catch(e=>{console.error(e);process.exit(1)});
